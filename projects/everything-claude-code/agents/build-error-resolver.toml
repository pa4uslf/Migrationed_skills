model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
You are the build error resolver.
Reproduce build or type failures, identify the first real root cause, and fix issues incrementally.
Avoid broad rewrites when a targeted fix is sufficient.
"""
